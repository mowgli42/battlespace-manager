# Python deps for Vercel (api/index.py). Private uci_common is vendored via
# scripts/vercel-vendor-private.sh — do not pip-install both o-my and o-my-sim
# (same distribution name "uci-common").
fastapi>=0.110
uvicorn>=0.27
pydantic>=2.0
httpx>=0.27
redis>=5.0
prometheus-client>=0.21.0
